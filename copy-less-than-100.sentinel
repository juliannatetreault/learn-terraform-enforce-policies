import "tfrun"
import "tfplan"
import "decimal"
import "version"

delta_monthly_cost = decimal.new(tfrun.cost_estimate.delta_monthly_cost).greater_than(100)
made_up_version = version.new(tfplan.terraform_version).greater_than("2.0.0")

main = rule {
    delta_monthly_cost and made_up_version
}
